<h1>New Wiki</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for wikis</p>
     <ul>
       <li>Stay on point.</li>
       <li>Use any letter in the alphabet you wish to use.</li>
       <li>The incessant use of editing will get you banned.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @wiki do |f| %>
     <% if wiki.errors.any? %>
     <div class="alert alert-danger">
       <h4><%= pluralize(wiki.errors.count, "error") %>.</h4>
       <ul>
         <% wiki.errors.full_messages.each do |msg| %>
           <li><%= msg %></li>
         <% end %>
       </ul>
     </div>
     <% end %>
     <%= form_group_tag(wiki.errors[:title]) do %>
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
     <% end %>
     <%= form_group_tag(wiki.errors[:body]) do %>
         <%= f.label :body %>
         <%= f.text_area :body, rows: 16, class: 'form-control', placeholder: "Enter wiki body" %>
     <% end %>
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>